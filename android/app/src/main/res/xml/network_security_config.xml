<?xml version="1.0" encoding="utf-8"?>
<!--
  HyperDrop Network Security Config
  
  Permits cleartext (HTTP) traffic to LAN IP addresses.
  Required because HyperDrop uses HTTP for device-to-device
  file transfers on the local network (no TLS on LAN).
-->
<network-security-config>
    <!-- Allow cleartext to private/LAN IP ranges -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.0.0</domain>
        <domain includeSubdomains="true">172.16.0.0</domain>
        <domain includeSubdomains="true">192.168.0.0</domain>
        <domain includeSubdomains="true">localhost</domain>
    </domain-config>
    
    <!-- Fallback: also allow cleartext globally for hotspot IPs
         that may not match standard private ranges -->
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
